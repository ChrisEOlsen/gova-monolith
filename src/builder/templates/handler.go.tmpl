package handlers

import (
	"net/http"

	"gova/app/cache"
	"gova/app/db"
)

func {{.PascalName}}{{.Method}}(database *db.DB, appCache *cache.Cache) http.HandlerFunc {
	return func(w http.ResponseWriter, r *http.Request) {
		// TODO: implement handler logic.
		// Use model methods — never write raw SQL here.
		// Auth, if required, is enforced by the RequireAuth wrap in
		// routes_gen.go; do not re-check it.
		jsonOK(w, nil)
	}
}
